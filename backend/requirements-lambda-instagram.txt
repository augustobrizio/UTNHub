# Dependencias minimas para la Lambda de ingesta de Instagram.
# Igual criterio que requirements-lambda-utn-web.txt: solo lo que toca
# run_ingesta_novedades para esta fuente, no el pyproject.toml completo.
sqlalchemy>=2.0
psycopg2-binary>=2.9
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
httpx>=0.28
langchain-openai>=0.3
boto3>=1.35
instagrapi>=2.1
